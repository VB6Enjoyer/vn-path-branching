1. We need to implement a modal to browse pre-existing flows.
2. Since Next.js `public` directory doesn't have an API to list files client-side, we need an API route.
   - Create `src/app/api/flows/route.ts` to scan `public/flows` or `flows` directory and return a list of available JSON files with basic parsed metadata.
3. Flow JSON metadata updates:
   - Export logic currently saves nodes, edges, activeTheme. We should add `title`, `author`, `timestamp`.
   - Update `Editor.tsx` settings panel to include inputs for `Flow Title` and `Flow Author`.
   - Update the export logic to embed these in the JSON.
4. Implement the "Load Flow" modal:
   - A button in the toolbar (maybe next to the 'Import' button) that says "Browse Flows".
   - It fetches from `/api/flows`.
   - Displays a modal with a CSS grid (3 columns).
   - Shows image placeholder (or uses theme background color), Title, Author, Timestamp, Node Count.
   - Provides filtering/sorting controls.
   - Clicking a flow fetches the raw JSON and loads it into the editor.
