The user wants decorative image nodes to bypass spoiler mode blurring since they are just decorations.
1. Check where `isBlurred` is passed or used.
2. In `Editor.tsx`, `const isBlurred = isSpoilerMode && node.id !== 'start' && !revealedNodeIds.has(node.id);`
   Change this to: `const isBlurred = isSpoilerMode && node.id !== 'start' && node.type !== 'image' && !revealedNodeIds.has(node.id);`
