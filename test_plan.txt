Okay, I see the bugs.
1. The regex for `--path-color` addition in Editor.tsx failed in my previous patch, so the CSS variable was never created in the `<style>` block, causing lines to disappear.
2. The regex for the localStorage metadata parse also failed, so title/author were never restored.

Plan:
1. Fix the CSS variables block in Editor.tsx to include `--path-color`.
2. Fix the localStorage parse block to restore `flow.metadata.title` and `author`.
3. Verify fixes.
