1. The user wants to rearrange the "Add Nodes" menu in the top-right toolbar.
2. Currently, the HTML looks like this:
   ```html
   <div className="flex justify-between items-center mb-1">
     <h3 className="font-bold text-sm text-gray-700 dark:text-gray-200">Add Nodes</h3>
     <div className="flex gap-1">
       ... buttons (Settings, View Endings, Lock, Spoiler Mode, Toggle Theme) ...
     </div>
   </div>
   ... lower buttons (Add node types, Import/Export, Auto layout, etc) ...
   ```
3. The user wants the icons/buttons in one row at the top, and "Add Nodes" text *below* them.
   So it should look like:
   ```html
   <div className="flex justify-end items-center mb-2">
     <div className="flex gap-1">
       ... buttons ...
     </div>
   </div>
   <h3 className="font-bold text-sm text-gray-700 dark:text-gray-200 mb-1 border-b border-gray-100 dark:border-gray-700 pb-1">Add Nodes</h3>
   ... lower buttons ...
   ```
4. This means moving the `<h3>` block out of the flex container and placing it below.

Plan:
- Modify `Editor.tsx` using python script to adjust the layout of the `top-right` toolbar Panel.
