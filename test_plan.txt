1. Implement a new state `showEndings` to toggle an endings tab.
2. Filter the `nodes` array for nodes of type `outcome` to get the list of endings.
3. Add a new button in the `top-right` toolbar Panel next to the Visual Settings button to toggle the Endings tab.
4. Render a new `<Panel position="top-right">` (positioned correctly next to the existing panels) when `showEndings` is true.
   - It will list all `outcome` nodes.
   - Each ending will have a button or click-handler to trigger `setHighlightedTargetId(node.id)` to highlight the path to it.
   - Also, clicking the ending should ideally pan the view to that node (we can use `useReactFlow().fitView` or `setCenter`).
